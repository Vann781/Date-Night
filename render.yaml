services:
  - type: web
    name: date-night-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn backend.app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: USE_MOCK_SEARCH
        value: "true"
